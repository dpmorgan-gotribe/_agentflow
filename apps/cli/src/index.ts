#!/usr/bin/env node
/**
 * @aigentflow/cli
 *
 * Command-line interface for Aigentflow.
 */

import { APP_NAME, APP_VERSION } from '@aigentflow/core';

console.log(`${APP_NAME} CLI v${APP_VERSION}`);
console.log('CLI will be implemented in a future step');
