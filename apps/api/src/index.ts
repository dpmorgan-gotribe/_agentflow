/**
 * @aigentflow/api
 *
 * NestJS API server for Aigentflow.
 */

import { APP_NAME, APP_VERSION } from '@aigentflow/core';

console.log(`${APP_NAME} API v${APP_VERSION}`);
console.log('API server will be implemented in Step 04-NESTJS-API');
